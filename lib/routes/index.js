/**
 * Module dependencies
 */

const auth = require('./auth');
const tasks = require('./tasks');
const users = require('./users');

/**
 * Module exports
 */

module.exports = {
    auth,
    tasks,
    users,
};
